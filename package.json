{
  "name": "wiki-note-worker",
  "version": "1.0.0",
  "description": "Stylish AI note generator on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 execute wiki_note --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
